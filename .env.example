# External Specs Storage Configuration
# Alternative: use .agentic-config.conf.yml (YAML format)
# Priority: Environment variables > .env > .agentic-config.conf.yml

# Repository URL for storing specification files externally
EXT_SPECS_REPO_URL=git@github.com:YOUR_USERNAME/YOUR_PROJECT--specs.git

# Local path where external specs will be cloned (gitignored)
EXT_SPECS_LOCAL_PATH=.specs
